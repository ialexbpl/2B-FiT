# Supabase Configuration (REQUIRED)
EXPO_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# AI Server Configuration (OPTIONAL - for AI Coach feature)
# The AI server runs separately from this app. See ai/DEPLOYMENT.md for full setup guide.
#
# For local development (emulator): http://localhost:8000
# For physical device on same WiFi: http://YOUR_PC_IP:8000 (e.g., http://192.168.1.100:8000)
# For production/APK: Your deployed server URL (e.g., https://your-ai-server.onrender.com)
EXPO_PUBLIC_AI_BASE_URL=http://localhost:8000

# Quick Start (Local):
# 1. Move the 'ai' folder to a separate location
# 2. Run: cd ai && setup.bat (first time) then start-server.bat
# 3. Find your PC's IP: ipconfig (look for IPv4 Address)
# 4. Set EXPO_PUBLIC_AI_BASE_URL=http://YOUR_IP:8000

# Google Places API (for nearby gyms search)
# Get your API key from: https://console.cloud.google.com/apis/credentials
# Enable "Places API" in your Google Cloud project
EXPO_PUBLIC_GOOGLE_PLACES_API_KEY=your-google-places-api-key-here

# Copy this file to .env and fill in your values
